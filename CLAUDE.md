# CLAUDE.md

The working agreement is **[AGENTS.md](AGENTS.md)**. Read it first — it is one
page: git across machines, long runs, command output, experiments, performance,
reporting, result tables, irreversible actions.

- `HANDOVER.md` — the engine's state and the measurements behind its defaults.
- `VERIFICATION.md` — known failure classes, and what would catch each.

Acknowledgement phrases, per-machine quirks and forms of address stay in your
own `~/.claude/CLAUDE.md`. Everything else that matters here is in `AGENTS.md`.
