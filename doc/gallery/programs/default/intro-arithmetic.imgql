// Arithmetic and comparison on numbers. Names are bound once and never
// reassigned: `c` below is a definition, not a variable being updated.
a = 7
b = 5
c = a * b - 3

print "c" c
print "c_greater_than_20" c > 20
