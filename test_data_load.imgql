// Simple test to verify data loading works
print "starting_simple_test" "Testing basic data loading..."

let data_dir = "/tmp/nnunet_synthetic_data"
let images_dir = data_dir + "/images"
let labels_dir = data_dir + "/labels"

// Load just a few files sequentially to avoid timing issues
let img_000 = simpleitk.ReadImage(images_dir + "/case_000_0000.nii.gz")
let lbl_000 = simpleitk.ReadImage(labels_dir + "/case_000.nii.gz")

print "first_files_loaded" "First files loaded successfully"

let img_001 = simpleitk.ReadImage(images_dir + "/case_001_0000.nii.gz")
let lbl_001 = simpleitk.ReadImage(labels_dir + "/case_001.nii.gz")

print "second_files_loaded" "Second files loaded successfully"

let img_012 = simpleitk.ReadImage(images_dir + "/case_012_0000.nii.gz")
let lbl_012 = simpleitk.ReadImage(labels_dir + "/case_012.nii.gz")

print "problem_file_loaded" "Case 012 loaded successfully!"

print "test_complete" "All test files loaded without errors"
