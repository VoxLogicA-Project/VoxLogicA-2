{
  "name": "voxlogica-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser UI for VoxLogicA 2. Built by build.mjs, which the Python server invokes; there is no dev server of its own.",
  "scripts": {
    "build": "node build.mjs --outdir dist",
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "esbuild-svelte": "^0.9.0",
    "svelte": "^5.0.0"
  },
  "dependencies": {
    "@niivue/niivue": "^0.69.0"
  }
}
