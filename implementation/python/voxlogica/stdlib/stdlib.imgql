// VoxLogicA Standard Library
// 
// This file defines common operator aliases and is automatically imported
// before any user code is processed.

// Import the default namespace to make basic primitives available
import "default"
import "vox1"

// print(label, value) function-style helper, alongside the print command.
let print(message, value) = print_primitive(message, value)
