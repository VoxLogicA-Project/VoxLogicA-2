// Debug the exact parameters being passed
print "debug_start" "Debugging parameter types..."

let images_dir = "/tmp/nnunet_synthetic_data/images"
let labels_dir = "/tmp/nnunet_synthetic_data/labels" 
let modalities = "T1"
let work_dir = "/tmp/nnunet_synthetic_workspace"
let dataset_id = 1
let dataset_name = "synthetic_squares_20cases"

print "debug_params" "All parameters set, calling train_directory..."

// Call with explicit parameters
let result = nnunet.train_directory(images_dir, labels_dir, modalities, work_dir, dataset_id, dataset_name)

print "debug_result" result
