// Test dataset.map with a simple constant function
import "dataset"
let files = dataset.readdir("/tmp/test_dataset_simple")
let add_ten(x) = 10
let result = dataset.map(files, add_ten)
print "result" result
