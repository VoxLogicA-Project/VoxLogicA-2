# Test Data

This directory is the canonical home for static test fixtures.

- `chris_t1.nii.gz`: baseline medical volume used by integration and examples.

Use `tests.data_registry` from Python tests to resolve paths, rather than hardcoding ad-hoc locations.
