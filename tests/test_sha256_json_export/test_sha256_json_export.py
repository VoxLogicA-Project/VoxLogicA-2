#!/usr/bin/env python3
"""
Tests that SHA256 IDs are correctly included in JSON exports. Verifies that each operation has a valid SHA256 ID, argument references are correct, goals reference valid IDs, and that IDs are deterministic and consistent with internal tracking.
"""
description = """Tests that SHA256 IDs are correctly included in JSON exports. Verifies that each operation has a valid SHA256 ID, argument references are correct, goals reference valid IDs, and that IDs are deterministic and consistent with internal tracking."""

# ...existing code from old test_sha256_json_export.py...
